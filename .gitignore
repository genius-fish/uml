# Mill build dir, plus rendered diagram artifacts: tests write to
# out/<format>/test and examples to out/<format>/examples.
out/

# Build / IDE metadata
.bsp/
.bloop/
.metals/
.scala-build/

# IntelliJ
.idea/
*.iws
*.iml
*.ipr

# Legacy sbt residue
target/
project/target/
project/project/
.cache

# OS
.DS_Store
Thumbs.db
*~

# Logs and crash dumps
*.log
hs_err_pid*

# Compiled bits
*.class

# Release state — produced by /gf-standards:release-prepare, consumed by
# /gf-standards:release-finalize. Lives only on the local working tree.
.release-state.json
